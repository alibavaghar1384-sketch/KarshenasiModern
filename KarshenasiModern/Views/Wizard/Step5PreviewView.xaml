<UserControl x:Class="KarshenasiModern.Views.Wizard.Step5PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:KarshenasiModern.Views.Controls"
             xmlns:conv="clr-namespace:KarshenasiModern.Converters"
             xmlns:vconv="clr-namespace:KarshenasiModern.Views.Converters"
             FlowDirection="RightToLeft"
             Background="#F5F6FA">

    <UserControl.Resources>
        <conv:BodyPartStatusConverter x:Key="StatusConverter"/>
        <vconv:BodyPartStatusToBrushConverter x:Key="StatusToBrushConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="PrintableArea" 
                    Background="White" 
                    Width="794" 
                    BorderBrush="#DDDDDD" 
                    BorderThickness="1" 
                    Padding="15,10,15,10">

                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="8" Opacity="0.1"/>
                </Border.Effect>

                <StackPanel>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <Image x:Name="LogoImage" Width="50" Height="50" Stretch="Uniform"/>
                            <TextBlock Text="کارشناسی مدرن" FontWeight="Bold" FontSize="11" Margin="0,3,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="گزارش کارشناسی خودرو" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <Grid Grid.Column="2" VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="شماره پرونده:" FontSize="11" FontWeight="Bold" Margin="0,1,10,1"/>
                            <TextBlock x:Name="ReportNumberText" FontSize="11" Grid.Column="1" Margin="0,1,0,1"/>

                            <TextBlock Text="تاریخ:" FontSize="11" Grid.Row="1" FontWeight="Bold" Margin="0,1,10,1"/>
                            <TextBlock x:Name="InspectionDateText" FontSize="11" Grid.Row="1" Grid.Column="1" Margin="0,1,0,1"/>

                            <TextBlock Text="کارشناس:" FontSize="11" Grid.Row="2" FontWeight="Bold" Margin="0,1,10,1"/>
                            <TextBlock x:Name="ExpertNameText" Text="کارشناس ۱" FontSize="11" Grid.Row="2" Grid.Column="1" Margin="0,1,0,1"/>
                        </Grid>
                    </Grid>

                    <Separator Margin="0,0,0,10" Background="#EEEEEE"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="0,3">
                            <TextBlock Text="نام مشتری: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="CustomerText" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" Margin="0,3">
                            <TextBlock Text="مدل خودرو: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="ModelText" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="2" Margin="0,3">
                            <TextBlock Text="پلاک: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="PlateText" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Margin="0,3">
                            <TextBlock Text="شماره تماس: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="PhoneText" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Margin="0,3">
                            <TextBlock Text="برند: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="BrandText" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,3">
                            <TextBlock Text="شماره شاسی: " FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="ChassisText" FontSize="12"/>
                        </StackPanel>
                    </Grid>

                    <Border BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="4" Padding="8" Background="#FAFAFA" Margin="0,0,0,10">
                        <UniformGrid Columns="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="کارکرد: " FontSize="11" FontWeight="SemiBold" Foreground="#555555"/>
                                <TextBlock x:Name="MileageText" FontSize="11" FontWeight="Bold" Foreground="#111111"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="موتور: " FontSize="11" FontWeight="SemiBold" Foreground="#555555"/>
                                <TextBlock x:Name="EngineStatusText" FontSize="11" FontWeight="Bold" Foreground="#1C61E7"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="گیربکس: " FontSize="11" FontWeight="SemiBold" Foreground="#555555"/>
                                <TextBlock x:Name="GearboxStatusText" FontSize="11" FontWeight="Bold" Foreground="#1C61E7"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="شاسی: " FontSize="11" FontWeight="SemiBold" Foreground="#555555"/>
                                <TextBlock x:Name="ChassisStatusText" FontSize="11" FontWeight="Bold" Foreground="#1C61E7"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="ایربگ: " FontSize="11" FontWeight="SemiBold" Foreground="#555555"/>
                                <TextBlock x:Name="AirbagStatusText" FontSize="11" FontWeight="Bold" Foreground="#1C61E7"/>
                            </StackPanel>
                        </UniformGrid>
                    </Border>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.4*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="دیاگرام وضعیت بدنه" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" Foreground="#333333"/>
                            <Border BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="4" Padding="2" Background="#FAFAFA" Height="240">
                                <controls:CarBodyDiagram x:Name="CarDiagram" Width="430" Height="230" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="تصاویر ثبت شده" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" Foreground="#333333"/>
                            <Border BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="4" Padding="5" Background="#FFFFFF" Height="240">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="DynamicPhotosControl">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#DDD" BorderThickness="1" Margin="3" Width="100" Height="70" CornerRadius="4" Background="#FAFAFA" ClipToBounds="True">
                                                    <Grid>
                                                        <Image Source="{Binding ImageSource}" Stretch="UniformToFill"/>
                                                        <Border Background="#B3000000" HorizontalAlignment="Left" VerticalAlignment="Bottom" Padding="4,2" CornerRadius="0,4,0,0">
                                                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="9" FontWeight="Bold"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <DataGrid x:Name="BodyPartsGrid" 
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                              HorizontalGridLinesBrush="#E5E7EB" Background="White" RowBackground="White" 
                              AlternatingRowBackground="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"
                              RowHeight="32" FontSize="12" Foreground="#1F2937" Margin="0,0,0,10">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="5,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="قطعه بدنه" Binding="{Binding PartName}" Width="*" >
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="وضعیت فعلی" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="BadgeBorder" Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" CornerRadius="8" Padding="10,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock x:Name="BadgeText" Text="{Binding Status, Converter={StaticResource StatusConverter}}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Healthy">
                                                <Setter TargetName="BadgeBorder" Property="BorderBrush" Value="#D1D9E6"/>
                                                <Setter TargetName="BadgeBorder" Property="BorderThickness" Value="1"/>
                                                <Setter TargetName="BadgeText" Property="Foreground" Value="#333333"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="ضخامت رنگ (میکرون)" Binding="{Binding PaintThickness}" Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#4B5563"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="توضیحات کارشناس:" FontWeight="Bold" FontSize="13" Margin="0,0,0,5" Foreground="#374151"/>
                            <TextBlock x:Name="DescriptionText" FontSize="12" TextWrapping="Wrap" LineHeight="20" Foreground="#4B5563"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="نتیجه نهایی:" FontWeight="Bold" FontSize="13" Margin="0,0,0,5" Foreground="#374151"/>
                            <Border x:Name="FinalResultCard" CornerRadius="6" Padding="8" BorderThickness="1" BorderBrush="#DDDDDD" Background="#FAFAFA">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="FinalResultIcon" FontSize="16" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="FinalResultText" Grid.Column="1" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" LineHeight="20" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="مهر و امضای کارشناس" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                        <TextBlock Grid.Column="1" Text="مهر شرکت کارشناسی مدرن" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    </Grid>

                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="BackButton" Content="مرحله قبل" Width="150" Height="40" Margin="0,0,10,0" Background="#E0E0E0" BorderThickness="0"/>
                <Button x:Name="PrintButton" Content="چاپ گزارش (PDF)" Width="150" Height="40" Margin="0,0,10,0" Background="#1C61E7" Foreground="White" BorderThickness="0"/>
                <Button x:Name="FinishButton" Content="ثبت نهایی و خروج" Width="150" Height="40" Background="#34C759" Foreground="White" BorderThickness="0"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>